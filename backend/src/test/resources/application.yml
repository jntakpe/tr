spring:
  jpa:
    properties:
          hibernate.cache.use_second_level_cache: false
  datasource:
    url: jdbc:h2:mem:trtestdb;DB_CLOSE_DELAY=-1
  jackson:
    serialization:
      write_dates_as_timestamps: false
  cache:
    type: none